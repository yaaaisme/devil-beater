
    -- Wait until the game loads
    if not game:IsLoaded() then
        game.Loaded:Wait()
    end

    -- Roblox Services
    local MarketplaceService = game:GetService("MarketplaceService")
    local SoundService = game:GetService("SoundService")
    local RunService = game:GetService("RunService")
    local VirtualUser = game:GetService("VirtualUser")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local ContextActionService = game:GetService("ContextActionService")
    local StarterGui = game:GetService("StarterGui")
    local UserInputService = game:GetService("UserInputService")
    local Http = game:GetService("HttpService")
    local TPS = game:GetService("TeleportService")
    local Players = game:GetService("Players")
    local PlayerGui = Players.LocalPlayer:WaitForChild("PlayerGui")
    local VirtualInputManager = game:GetService("VirtualInputManager")
    local Lighting = game:GetService("Lighting")
    local Workspace = game:GetService("Workspace")
    local Api = "https://games.roblox.com/v1/games/"

    -- Variables
    local LocalPlayer = Players.LocalPlayer
    local Character = LocalPlayer.Character
    local PlaceId = game.PlaceId
    local JobId = game.JobId



    -- Main Script Module
    local ScriptValues = {
        ScriptVersion = " v1",
        ScriptOwner = "Syphix",
        FolderName = "Keido ui (Fluent)",
        SettingsFile = "CoS_Loader.json",
        AutoLoadSettings = getgenv().AutoLoadConfigurations or false,
        NotificationSoundId = 1760921747,
        WarningSoundId = 3165700530,
        ErrorSoundId = 5188022160,
        SupportedExecutors = {"Synapse X", "Codex", "Delta", "Electron", "Fluxus", "Valyse"},
        ExecutorsIdentifier = {
            "Fingerprint",
            "Krnl-Fingerprint",
            "Syn-Fingerprint",
            "SW-Fingerprint",
            "Flux-Fingerprint",
            "Valyse-Fingerprint",
            "Electron-Fingerprint",
            "Delta-Figerprint"
        },
        DisconnectTypes = {{"ban", {"ban", "perm"}}, {"network", {"internet connection", "network"}}},
        AdministratorRoles = {"💎Administrator", "⭐Friends/Family", "Testing", "️🛠️Developer", "👑Co-Owner", "👑Owner"},
        RoleCaution = {"❤️Contributor", "📝Tester", "Community Artist", "🎥Content Creator"}
    }

    local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
    local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
    local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

    local Options = Fluent.Options

    local Window = Fluent:CreateWindow({
        Title = "Keido ui | Devil beater".. ScriptValues.ScriptVersion .."",
        SubTitle = "by ".. ScriptValues.ScriptOwner .."",
        TabWidth = 160,
        Size = UDim2.fromOffset(580, 400),
        Acrylic = true, 
        Theme = "Dark",
        MinimizeKey = Enum.KeyCode.P    
    })

    if not _G.AlreadyExecuted == true then
        local screenGui = Instance.new("ScreenGui")
        screenGui.Parent = game.CoreGui

        local CloseButton = Instance.new("ImageButton")
        CloseButton.Parent = screenGui
        CloseButton.Position = UDim2.new(0.01, 0, 0.3, 0)
        CloseButton.Size = UDim2.new(0, 43, 0, 43)
        CloseButton.BackgroundTransparency = 0
        CloseButton.Image = "rbxassetid://16614324610"
        CloseButton.BackgroundColor3 = Color3.new(0, 0, 0)
        CloseButton.BorderColor3 = Color3.new(1, 1, 1)
        CloseButton.ClipsDescendants = true

        local UICorner = Instance.new("UICorner")
        UICorner.Parent = CloseButton

        local UIStroke = Instance.new("UIStroke")
        UIStroke.Parent = CloseButton
        UIStroke.Thickness = 2

        CloseButton.MouseButton1Click:Connect(
            function()
                VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Q, false, game)
            end
        )       
    end

    _G.AlreadyExecuted = true

    --Tabs
    --tabs icons : https://github.com/dawid-scripts/Fluent/blob/master/src/Icons.lua
    local Tabs = {
        Main = Window:AddTab({ Title = "General", Icon = "home" }),
        Acts = Window:AddTab({ Title = "Act Selector", Icon = "library" }),
        Specific = Window:AddTab({ Title = "Specific Characters", Icon = "archive" }),
        NonRelated = Window:AddTab({ Title = "Non-db related", Icon = "folder" }),
        Others = Window:AddTab({ Title = "Others", Icon = "settings" }),
    }
    
----------------

    SaveManager:SetLibrary(Fluent)
    InterfaceManager:SetLibrary(Fluent)

    SaveManager:IgnoreThemeSettings()

    SaveManager:SetIgnoreIndexes({})

    Window:SelectTab(1)
    SaveManager:BuildConfigSection(Tabs.Others)

    local NotificationSound
    local WarningSound
    local ErrorSound

    NotificationSound = Instance.new("Sound")
    NotificationSound.Name = "NotificationSound"
    NotificationSound.SoundId = "rbxassetid://" .. ScriptValues.NotificationSoundId
    NotificationSound.Volume = 2
    NotificationSound.Parent = SoundService

    WarningSound = Instance.new("Sound")
    WarningSound.Name = "WarningSound"
    WarningSound.SoundId = "rbxassetid://" .. ScriptValues.WarningSoundId
    WarningSound.Volume = 2
    WarningSound.Parent = SoundService

    ErrorSound = Instance.new("Sound")
    ErrorSound.Name = "ErrorSound"
    ErrorSound.SoundId = "rbxassetid://" .. ScriptValues.ErrorSoundId
    ErrorSound.Volume = 2
    ErrorSound.Parent = SoundService

    local function Notification(SoundType, Text)
        if SoundType == "Notification" then
            NotificationSound:Play()
            Fluent:Notify({
                Title = "Notification",
                Content = Text,
                Duration = 5
            })
        elseif SoundType == "Warning" then
            WarningSound:Play()
            Fluent:Notify({
                Title = "Warning",
                Content = Text,
                Duration = 5
            })
        elseif SoundType == "Error" then
            ErrorSound:Play()
            Fluent:Notify({
                Title = "Error",
                Content = Text,
                Duration = 5
            })
        end
    end

    -- Functions



---------------


    -- UI Variables

----------------


    local NoCdKeybind = Tabs.Main:AddKeybind("NoCd", {
        Title = "No cooldown",
        Mode = "Toggle", -- Always, Toggle, Hold
        Default = "T", -- String as the name of the keybind (MB1, MB2 for mouse buttons)

        -- Occurs when the keybind is clicked, Value is `true`/`false`
        Callback = function(Value)
        local player = game:GetService("Players").LocalPlayer
        local move = player.PlayerGui.CombatGui.Scale.CooldownFrame.Moves["4"].Cooldown
        if move then
            move.Name = "T"
        end
        end,

        -- Occurs when the keybind itself is changed, `New` is a KeyCode Enum OR a UserInputType Enum
        ChangedCallback = function(New)            
        
        end
    })

    Tabs.Main:AddButton({
        Title = "Run L to autoskip script",
        Description = "no need to click again",
        Callback = function()
            local isHolding = false
-- Detect when the key is pressed
UserInputService.InputBegan:Connect(function(input, isProcessed)
    if isProcessed then return end -- Ignore inputs processed by the game/UI
    if input.KeyCode == Enum.KeyCode.L then
        isHolding = true
        -- Start a loop while holding
        while isHolding do
        local args = {[1] = "SkipCutscene"}
            game:GetService("ReplicatedStorage"):WaitForChild("ActModuleEvent"):FireServer(unpack(args))
            task.wait(0.1) -- Adjust loop speed
        end
    end
end)
-- Detect when the key is released
UserInputService.InputEnded:Connect(function(input, isProcessed)
    if isProcessed then return end
    if input.KeyCode == Enum.KeyCode.L then
        isHolding = false
    end
end)
        end
    })


    Tabs.Main:AddButton({
        Title = "Run H to spam demix script",
        Description = "no need to click again",
        Callback = function()
            local isHolding = false
            -- Detect when the key is pressed
            UserInputService.InputBegan:Connect(function(input, isProcessed)
                if isProcessed then return end -- Ignore inputs processed by the game/UI
                if input.KeyCode == Enum.KeyCode.H then
        isHolding = true
        -- Start a loop while holding
        while isHolding do
        local args = {[1] = "Nota",[2] = 528649935}
            game:GetService("Players").LocalPlayer.Character.Demixeur.RemoteEvent:FireServer(unpack(args))
            task.wait(0.1) -- Adjust loop speed
        end
                end
            end)
            UserInputService.InputBegan:Connect(function(input, isProcessed)
                if isProcessed then return end -- Ignore inputs processed by the game/UI
                if input.KeyCode == Enum.KeyCode.H then
                    isHolding = true
                    -- Start a loop while holding
                    while isHolding do
                        local args = {[1] = "Capella",[2] = 528649935}
                        game:GetService("Players").LocalPlayer.Character.Demixeur.RemoteEvent:FireServer(unpack(args))
                        task.wait() -- Adjust loop speed
                    end
                end
            end)
            -- Detect when the key is released
            UserInputService.InputEnded:Connect(function(input, isProcessed)
                if isProcessed then return end
                if input.KeyCode == Enum.KeyCode.H then
                    isHolding = false
                end
            end)
        end
    })

    Tabs.Main:AddButton({
        Title = "Run H to spam J.J. script",
        Description = "no need to click again also",
        Callback = function()
            local isHolding = false
-- Detect when the key is pressed
UserInputService.InputBegan:Connect(function(input, isProcessed)
    if isProcessed then return end -- Ignore inputs processed by the game/UI
    if input.KeyCode == Enum.KeyCode.H then
        isHolding = true
        -- Start a loop while holding
        while isHolding do
            local args = {[1] = "Finisher"}
            game:GetService("Players").LocalPlayer.Character:FindFirstChild("J.J.").RemoteEvent:FireServer(unpack(args))
            task.wait() -- Adjust loop speed
        end
    end
end)
-- Detect when the key is released
UserInputService.InputEnded:Connect(function(input, isProcessed)
    if isProcessed then return end
    if input.KeyCode == Enum.KeyCode.H then
        isHolding = false
    end
end)
        end
    })

    Tabs.Acts:AddParagraph({
        Title = "Everything here works with a party btw",
        Content = "must be in lobby to tp"
    })


    local AdventDropdown = Tabs.Acts:AddDropdown("Dropdown", {
        Title = "Advent select",
        Values = {"Shadow Society", "Mythology", "Heroes of Light", "Rotomo Family Holiday", "Gold Rush", "The Laboratory(unfinished)", "Testing Realm"},
        Multi = false,
        Default = nil,
    })


    AdventDropdown:OnChanged(function(Value)
        if Value == "Shadow Society" then 
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer("TeleportParty", 3124999767, true, 8543541285)
        elseif Value == "Mythology" then 
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer("TeleportParty", 3124999767, true, 6930614828)
        elseif Value == "Heroes of Light" then 
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer("TeleportParty", 3124999767, true, 7169453216)
        elseif Value == "Rotomo Family Holiday" then 
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer("TeleportParty", 3124999767, true, 7550694574)
        elseif Value == "Gold Rush" then 
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer("TeleportParty", 3124999767, true, 14369886144)
        elseif Value == "The Laboratory(unfinished)" then 
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer("TeleportParty", 3124999767, true, 7328473256)
        elseif Value == "Testing Realm" then 
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer("TeleportParty", 8830701703, true)
        end
    end)

    local tpSky = Tabs.Acts:AddButton({
        Title = "Sky Island teleport", 
        Callback = function()
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer("TeleportParty", 3124999767, true, {-1, })
    end})


    local RejoinServer = Tabs.NonRelated:AddButton({
        Title = "Rejoin Server", 
        Callback = function()
            game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end})

    local unlockZoom = Tabs.NonRelated:AddButton({
        Title = "Unlock zoom", 
        Callback = function()
            game:GetService("Players").LocalPlayer.CameraMaxZoomDistance = 99999999
    end})

    local Yield = Tabs.NonRelated:AddButton({
        Title = "Infinite Yield", 
        Callback = function()
            loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
    end})

    local universeView = Tabs.NonRelated:AddButton({
        Title = "Universe Viewer", 
        Callback = function()
            loadstring(game:HttpGet("https://pastebin.com/raw/KmiuAFZh"))()
    end})

    local Dex = Tabs.NonRelated:AddButton({
        Title = "Dex explorer", 
        Callback = function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/infyiff/backup/main/dex.lua"))()
    end})

    local FlyV3 = Tabs.NonRelated:AddButton({
        Title = "Fly v3", 
        Callback = function()
            loadstring(game:HttpGet('https://pastebin.com/raw/YSL3xKYU'))()
    end})


    local kiwiKeidoSpecific = Tabs.Specific:AddButton({
        Title = "Kiwi Keido", 
        Callback = function()
            local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()
            local Window = Library:NewWindow("Kiwi Keido")
            local Tab1 = Window:NewSection("Abilities spam")
                Tab1:CreateButton("Lash (Keypad9)", function()
                    local isHolding = false
                        -- Detect when the key is pressed
                        UserInputService.InputBegan:Connect(function(input, isProcessed)
                            if isProcessed then return end -- Ignore inputs processed by the game/UI
                            if input.KeyCode == Enum.KeyCode.KeypadNine then
                                isHolding = true
                                -- Start a loop while holding
                                while isHolding do
                                    local args = {[1] = "Lash"}
                                    game:GetService("Players").LocalPlayer.Character:FindFirstChild("Kiwi Keido").RemoteEvent:FireServer(unpack(args))
                                    task.wait() -- Adjust loop speed
                                end
                            end
                        end)
                        -- Detect when the key is released
                        UserInputService.InputEnded:Connect(function(input, isProcessed)
                            if isProcessed then return end
                            if input.KeyCode == Enum.KeyCode.KeypadNine then
                                isHolding = false
                            end
                        end)                    
                end)
                Tab1:CreateButton("Cash (Keypad8)", function()
                    local isHolding = false
                        -- Detect when the key is pressed
                        UserInputService.InputBegan:Connect(function(input, isProcessed)
                            if isProcessed then return end -- Ignore inputs processed by the game/UI
                            if input.KeyCode == Enum.KeyCode.KeypadEight then
                                isHolding = true
                                -- Start a loop while holding
                                while isHolding do
                                    local args = {[1] = "Cash"}
                                    game:GetService("Players").LocalPlayer.Character:FindFirstChild("Kiwi Keido").RemoteEvent:FireServer(unpack(args))
                                    task.wait() -- Adjust loop speed
                                end
                            end
                        end)
                        -- Detect when the key is released
                        UserInputService.InputEnded:Connect(function(input, isProcessed)
                            if isProcessed then return end
                            if input.KeyCode == Enum.KeyCode.KeypadEight then
                                isHolding = false
                            end
                        end)                    
                end)        

    end})

    local DemixeurSpecific = Tabs.Specific:AddButton({
        Title = "Demixeur", 
        Callback = function()
            local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()
            local Window = Library:NewWindow("Demixeur")
            local Tab1 = Window:NewSection("Abilities spam")
                Tab1:CreateButton("E and R (Keypad9)", function()
                    local isHolding = false
                    -- Detect when the key is pressed
                    UserInputService.InputBegan:Connect(function(input, isProcessed)
                        if isProcessed then return end -- Ignore inputs processed by the game/UI
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                isHolding = true
                -- Start a loop while holding
                while isHolding do
                local args = {[1] = "Nota",[2] = 528649935}
                    game:GetService("Players").LocalPlayer.Character.Demixeur.RemoteEvent:FireServer(unpack(args))
                    task.wait(0.1) -- Adjust loop speed
                end
                end
            end)
            UserInputService.InputBegan:Connect(function(input, isProcessed)
                if isProcessed then return end -- Ignore inputs processed by the game/UI
                if input.KeyCode == Enum.KeyCode.KeypadNine then
                    isHolding = true
                    -- Start a loop while holding
                    while isHolding do
                        local args = {[1] = "Capella",[2] = 528649935}
                        game:GetService("Players").LocalPlayer.Character.Demixeur.RemoteEvent:FireServer(unpack(args))
                        task.wait() -- Adjust loop speed
                    end
                end
            end)
            -- Detect when the key is released
            UserInputService.InputEnded:Connect(function(input, isProcessed)
                if isProcessed then return end
                if input.KeyCode == Enum.KeyCode.KeypadNine then
                    isHolding = false
                end
            end)         
                end)
       

    end})

    local JJSpecific = Tabs.Specific:AddButton({
        Title = "J.J.", 
        Callback = function()
            local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()
            local Window = Library:NewWindow("J.J.")
            local Tab1 = Window:NewSection("Abilities spam")
                Tab1:CreateButton("Lash (Keypad9)", function()
                    local isHolding = false
                    -- Detect when the key is pressed
                    UserInputService.InputBegan:Connect(function(input, isProcessed)
                        if isProcessed then return end -- Ignore inputs processed by the game/UI
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                            isHolding = true
                            -- Start a loop while holding
                            while isHolding do
                                local args = {[1] = "Finisher"}
                                game:GetService("Players").LocalPlayer.Character:FindFirstChild("J.J.").RemoteEvent:FireServer(unpack(args))
                                task.wait() -- Adjust loop speed
                            end
                        end
                    end)
                    -- Detect when the key is released
                    UserInputService.InputEnded:Connect(function(input, isProcessed)
                        if isProcessed then return end
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                            isHolding = false
                        end
                    end)                    
                end)        
    end})

    local TerrySpecific = Tabs.Specific:AddButton({
        Title = "Terry", 
        Callback = function()
            local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()
            local Window = Library:NewWindow("Terry")
            local Tab1 = Window:NewSection("Abilities spam")
                Tab1:CreateButton("Burning Fist (Keypad9)", function()
                    local isHolding = false
                    -- Detect when the key is pressed
                    UserInputService.InputBegan:Connect(function(input, isProcessed)
                        if isProcessed then return end -- Ignore inputs processed by the game/UI
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                            isHolding = true
                            -- Start a loop while holding
                            while isHolding do
                                local args = {[1] = "BurningFist"}
                                game:GetService("Players").LocalPlayer.Character.Terry.RemoteEvent:FireServer(unpack(args))
                                task.wait() -- Adjust loop speed
                            end
                        end
                    end)
                    -- Detect when the key is released
                    UserInputService.InputEnded:Connect(function(input, isProcessed)
                        if isProcessed then return end
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                            isHolding = false
                        end
                    end)                    
                end)        
    end})

    local XorSpecific = Tabs.Specific:AddButton({
        Title = "Xor", 
        Callback = function()
            local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()
            local Window = Library:NewWindow("Xor")
            local Tab1 = Window:NewSection("Abilities spam")
                Tab1:CreateButton("Nova (Keypad9)", function()
                    local isHolding = false
                    -- Detect when the key is pressed
                    UserInputService.InputBegan:Connect(function(input, isProcessed)
                        if isProcessed then return end -- Ignore inputs processed by the game/UI
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                            isHolding = true
                            -- Start a loop while holding
                            while isHolding do
                                local player = game:GetService("Players").LocalPlayer
                                local mouse = player:GetMouse()
                                local args = {[1] = "Nova",[2] = 1,[3] = mouse.Hit.p}
                                player.Character.XOR.RemoteEvent:FireServer(unpack(args))
                                task.wait() -- Adjust loop speed
                            end
                        end
                    end)
                    -- Detect when the key is released
                    UserInputService.InputEnded:Connect(function(input, isProcessed)
                        if isProcessed then return end
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                            isHolding = false
                        end
                    end)                    
                end)        
    end})

    local NickSpecific = Tabs.Specific:AddButton({
        Title = "King Unmasked", 
        Callback = function()
            local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()
            local Window = Library:NewWindow("King Unmasked")
            local Tab1 = Window:NewSection("Abilities spam")
                Tab1:CreateButton("Incudob-Colpo (Keypad9)", function()
                    local isHolding = false
                    -- Detect when the key is pressed
                    UserInputService.InputBegan:Connect(function(input, isProcessed)
                        if isProcessed then return end -- Ignore inputs processed by the game/UI
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                            isHolding = true
                            -- Start a loop while holding
                            while isHolding do
                                local args = {[1] = "Incubo"}
                                game:GetService("Players").LocalPlayer.Character:FindFirstChild("KING Unmasked").RemoteEvent:FireServer(unpack(args))
                                wait(0.5)
                                local args = {[1] = "Colpo"}
                                game:GetService("Players").LocalPlayer.Character:FindFirstChild("KING Unmasked").RemoteEvent:FireServer(unpack(args))
                                task.wait() -- Adjust loop speed
                            end
                        end
                    end)
                    -- Detect when the key is released
                    UserInputService.InputEnded:Connect(function(input, isProcessed)
                        if isProcessed then return end
                        if input.KeyCode == Enum.KeyCode.KeypadNine then
                            isHolding = false
                        end
                    end)                    
                end)        
    end})

    local AntiAfk = Tabs.Others:AddButton({
        Title = "Anti Afk", 
        Callback = function()
            NotificationSound:Play()
            LocalPlayer.Idled:connect(
                function()
                    VirtualUser:CaptureController()
                    VirtualUser:ClickButton2(Vector2.new())
                end
            )
    end})


    local LowQualityTextures = Tabs.Others:AddButton({
        Title = "Low Quality Textures", 
        Callback = function()
            NotificationSound:Play()
            for _, v in pairs(game:GetService("Workspace"):GetDescendants()) do
                if v:IsA("BasePart") and not v.Parent:FindFirstChild("Humanoid") then
                    v.Material = "SmoothPlastic"
                    if v:IsA("Texture") then
                        v:Destroy()
                    end
                end
            end
    end})

    NotificationSound:Play()
    Fluent:Notify({
        Title = "Notification",
        Content = "Script Made By syphix_n19",
        Duration = 5
    })
